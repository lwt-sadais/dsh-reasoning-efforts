import type { Context } from '@deepseek-ai/cordis'

export const name = 'dsh-reasoning-efforts'

/**
 * 保持 Host 入口可组合；插件业务完全位于客户端设置页。
 * @param _ctx 当前 Cordis Host 上下文。
 */
export function apply(_ctx: Context): void {}
